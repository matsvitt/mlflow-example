name: mlexample

conda_env: conda_env.yml

entry_points:
  main:
    parameters:
      alpha: {type: float, default: 0.5}
      l1_ratio: {type: float, default: 0.1}
    command: "python train_multiclass.py {alpha} {l1_ratio}"
